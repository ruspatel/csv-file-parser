#include <stdlib.h>
#include <stdio.h>
#include <iostream>

class parser{
    public:
        parser();
        ~parser();
        int findNan(char* fileName);
        int calculateMean();
        char* readLine(int lineNumber);


    private:

};